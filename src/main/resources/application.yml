server:
  port: 8080
  tomcat:
    relaxed-query-chars: "[,]"
    uri-encoding: UTF-8
    connection-timeout: 20000

spring:
  redis:
    host: locahost
    port: 6379
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/sample_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: robot123#
    #JPA
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true